export default class Item {
    constructor(menu, name, element) {
        this.menu = menu;
        this.name = name;
        this.element = element;
    }

    get checked() {
    }

    set checked(value) {
    }

    get visible() {
    }

    set visible(value) {
    }
};